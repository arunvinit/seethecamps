<% layout('layouts/boilerplate')%>
    <!-- <h1>Edit campground</h1>
    <form action="/campground/<%=camp._id%>?_method=PUT" method="post">
        <label for="title">Title </label>
        <input type="text" name="campground[title]" required placeholder="Title" id="title" value="<%=camp.title%>">
        <label for="location">Location</label>
        <input type="text" id="location" name="campground[location]" required placeholder="location" value="<%=camp.location%>">
        <button>Edit Campground</button>
    </form> -->


    <div class="row">
        <h1 class="text-center">Edit Campground</h1>
        <div class="col-6 offset-3">
            <form action="/campground/<%=camp._id%>?_method=PUT" method="post" novalidate class="validated-form" enctype="multipart/form-data">
                <div class="mb-3">
                <label for="title" class="form-label">Title </label>
                <input type="text" class="form-control" name="campground[title]" required placeholder="Title" id="title"  value="<%=camp.title%>">
                </div>
                <div class="mb-3">
                <label for="location" class="form-label">Location</label>
                <input type="text" class="form-control" id="location" name="campground[location]" required placeholder="location"  value="<%=camp.location%>">
                </div>
                <label for="inputGroupFile04" class="mb-3">Upload images</label>
                <div class="input-group mb-3">
                    <input type="file" class="form-control" id="inputGroupFile04" aria-describedby="inputGroupFileAddon04" aria-label="Upload" name="image" multiple>
                </div>
                <div class="mb-3">
                    <% camp.images.forEach(function(img, i) {  %>
                        <img src="<%=img.url %>" class="img-thumbnail" alt="">
                        <div class="form-check-inline">
                            <input type="checkbox" id="image-<%=i%>" name="deleteImages[]" value="<%=img.filename%>">
                        </div>
                        <label for="image-<%=i%>">Delete?</label>
                        <% })%>
                </div>
                <div class="mb-3">
                    <label for="price" class="form-label">Campground Price</label>
                    <div class="input-group">
                        <span class="input-group-text" id="price-label">$</span>
                        <input type="text" class="form-control" id="price" name="campground[price]" required placeholder="0.00" aria-describedby="price-label"  value="<%=camp.price%>">
                    </div>
                </div>
                <div class="mb-3">
                    <label for="description" class="form-label">Description</label>
                    <textarea type="text" class="form-control" id="description" name="campground[description]" required placeholder="Description"> <%=camp.description%></textarea>
                </div>
                <button class="btn btn-info mb-3">Update This Campground</button>
            </form>
            <a class="mb-3" href="/campground/<%=camp._id%>">Back to Campground</a>
        </div>
        
    </div>
    


    